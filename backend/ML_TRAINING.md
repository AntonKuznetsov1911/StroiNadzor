# ML Training Guide - YOLOv8 для обнаружения дефектов

Руководство по обучению модели YOLOv8 для автоматического обнаружения дефектов в строительстве.

---

## 📋 Требования

### Системные требования

- **Python:** 3.11+
- **CUDA:** 11.8+ (для GPU) - опционально
- **RAM:** 16+ GB (рекомендуется)
- **GPU:** NVIDIA GPU с 6+ GB VRAM (рекомендуется)
- **Disk:** 50+ GB свободного места

### Python зависимости

Установите все зависимости:

```bash
cd backend
pip install -r requirements.txt
```

Основные пакеты:
- `ultralytics==8.1.18` - YOLOv8
- `torch==2.2.0` - PyTorch
- `torchvision==0.17.0`
- `opencv-python==4.9.0.80`
- `pillow==10.2.0`

---

## 🗂️ Подготовка датасета

### Шаг 1: Создание структуры

Запустите скрипт для создания структуры:

```bash
python scripts/train_yolo.py --mode prepare
```

Создастся следующая структура:

```
data/defects/
├── images/
│   ├── train/      # Изображения для обучения (70-80%)
│   ├── val/        # Изображения для валидации (10-15%)
│   └── test/       # Изображения для тестирования (10-15%)
└── labels/
    ├── train/      # Аннотации для обучения
    ├── val/        # Аннотации для валидации
    └── test/       # Аннотации для тестирования
```

### Шаг 2: Добавление изображений

Добавьте изображения дефектов в соответствующие директории:

```bash
# Примерное распределение:
# - 70% в train/
# - 15% в val/
# - 15% в test/
```

**Рекомендации по изображениям:**
- Формат: JPG, PNG
- Разрешение: 640x640 или больше
- Минимум: 300+ изображений на класс
- Разнообразие: разные условия освещения, углы, дистанции

### Шаг 3: Создание аннотаций

Формат YOLO (TXT файл для каждого изображения):

```
<class_id> <x_center> <y_center> <width> <height>
```

Где все координаты нормализованы (0-1).

**Пример аннотации** (`image001.txt`):

```
0 0.5 0.5 0.3 0.1    # Класс 0 (crack) в центре
2 0.25 0.25 0.15 0.15  # Класс 2 (corrosion) слева вверху
```

**Инструменты для аннотации:**
- [LabelImg](https://github.com/heartexlabs/labelImg) - десктоп приложение
- [Roboflow](https://roboflow.com/) - онлайн платформа
- [CVAT](https://cvat.org/) - онлайн/self-hosted

### Классы дефектов

10 классов дефектов:

| ID | Класс | Описание | Русское название |
|----|-------|----------|------------------|
| 0 | crack | Трещины в бетоне, стенах | Трещина |
| 1 | spalling | Отслоение бетона | Отслоение |
| 2 | corrosion | Коррозия арматуры | Коррозия |
| 3 | deformation | Деформация конструкций | Деформация |
| 4 | leak | Протечки воды | Протечка |
| 5 | loose_material | Рыхлый материал | Рыхлый материал |
| 6 | settlement | Осадка фундамента | Осадка |
| 7 | misalignment | Несоответствие нормам | Несоответствие |
| 8 | damage | Механические повреждения | Повреждение |
| 9 | other | Другие дефекты | Другое |

---

## 🚀 Обучение модели

### Базовое обучение (Nano модель)

Самая быстрая модель, подходит для CPU:

```bash
python scripts/train_yolo.py \
  --mode train \
  --model-size n \
  --epochs 100 \
  --batch-size 16 \
  --image-size 640
```

### Продвинутое обучение (Medium модель)

Баланс между скоростью и точностью:

```bash
python scripts/train_yolo.py \
  --mode train \
  --model-size m \
  --epochs 200 \
  --batch-size 32 \
  --image-size 1024 \
  --device cuda
```

### Максимальная точность (Large модель)

Для мощных GPU:

```bash
python scripts/train_yolo.py \
  --mode train \
  --model-size l \
  --epochs 300 \
  --batch-size 64 \
  --image-size 1280 \
  --device cuda
```

### Параметры обучения

| Параметр | Описание | Дефолт | Рекомендации |
|----------|----------|--------|--------------|
| `--model-size` | Размер модели (n/s/m/l/x) | n | n для начала, m для production |
| `--epochs` | Количество эпох | 100 | 100-300, смотреть на val loss |
| `--batch-size` | Размер батча | 16 | Зависит от GPU (16-64) |
| `--image-size` | Размер изображения | 640 | 640-1280, больше = точнее |
| `--device` | Устройство | auto | cuda (GPU) или cpu |

---

## 📊 Валидация модели

После обучения валидируйте модель:

```bash
python scripts/train_yolo.py \
  --mode validate \
  --model-path runs/train/defect_detection/weights/best.pt \
  --data-dir data/defects
```

**Метрики:**
- **mAP50**: Mean Average Precision при IoU=0.5 (основная метрика)
- **mAP50-95**: mAP при IoU от 0.5 до 0.95
- **Precision**: Точность (TP / (TP + FP))
- **Recall**: Полнота (TP / (TP + FN))

**Целевые значения:**
- mAP50 > 0.7 - хорошая модель
- mAP50 > 0.8 - отличная модель
- mAP50 > 0.9 - превосходная модель

---

## 📦 Экспорт модели

### Экспорт в ONNX (рекомендуется)

```bash
python scripts/train_yolo.py \
  --mode export \
  --model-path runs/train/defect_detection/weights/best.pt \
  --export-format onnx
```

### Другие форматы

```bash
# TorchScript
--export-format torchscript

# CoreML (для iOS)
--export-format coreml

# TensorRT (для NVIDIA GPU)
--export-format engine

# TFLite (для mobile)
--export-format tflite
```

---

## 🔧 Использование обученной модели

### В Python коде

```python
from ultralytics import YOLO

# Загрузка модели
model = YOLO('runs/train/defect_detection/weights/best.pt')

# Предсказание на изображении
results = model('path/to/image.jpg')

# Получение результатов
for result in results:
    boxes = result.boxes  # Bounding boxes
    for box in boxes:
        cls = int(box.cls[0])  # Класс
        conf = float(box.conf[0])  # Уверенность
        xyxy = box.xyxy[0].tolist()  # Координаты [x1, y1, x2, y2]

        print(f"Класс: {cls}, Conf: {conf:.2f}, Bbox: {xyxy}")
```

### В FastAPI endpoint

Уже реализовано в `app/services/ml_service.py`:

```python
from app.services.ml_service import detect_defects

# Анализ изображения
defects = detect_defects(image_path)
```

---

## 🎯 Оптимизация модели

### 1. Augmentation

Модель уже использует augmentation:
- Horizontal flip (0.5 вероятность)
- HSV shift (цвет, насыщенность, яркость)
- Translation (0.1)
- Scale (0.5)

### 2. Hyperparameter tuning

Попробуйте разные learning rates:

```python
# В train_yolo.py можно изменить:
lr0=0.001  # Начальный LR (попробуйте 0.001, 0.01, 0.1)
lrf=0.01   # Финальный LR
```

### 3. Transfer learning

Используйте предобученную модель на похожем датасете:

```bash
# Вместо yolov8n.pt используйте свою модель
--model-path your_pretrained_model.pt
```

---

## 📈 Monitoring обучения

### TensorBoard

Запустите TensorBoard для мониторинга:

```bash
tensorboard --logdir runs/train
```

Откройте http://localhost:6006

**Метрики для отслеживания:**
- Train/Loss - должна снижаться
- Val/Loss - должна снижаться без overfitting
- Metrics/mAP50 - должна расти
- Learning Rate - график изменения LR

### Wandb (опционально)

Интеграция с Weights & Biases:

```bash
pip install wandb
wandb login

# При обучении автоматически логирует в Wandb
```

---

## 🐛 Troubleshooting

### CUDA Out of Memory

Уменьшите batch size:

```bash
--batch-size 8  # Было 16
```

Или уменьшите image size:

```bash
--image-size 416  # Было 640
```

### Модель не обучается (loss не падает)

1. Проверьте аннотации (правильность координат)
2. Увеличьте learning rate: `lr0=0.01`
3. Добавьте больше данных
4. Проверьте баланс классов

### Overfitting

Признаки:
- Train loss падает, val loss растет
- High mAP on train, low on val

Решения:
1. Добавьте больше данных
2. Увеличьте augmentation
3. Уменьшите model size
4. Добавьте dropout/regularization
5. Early stopping (patience=50)

### Low mAP

1. Обучайте дольше (больше epochs)
2. Используйте больший model size
3. Увеличьте image size
4. Улучшите качество аннотаций
5. Добавьте больше примеров сложных случаев

---

## 📚 Дополнительные ресурсы

### Документация
- [YOLOv8 Docs](https://docs.ultralytics.com/)
- [PyTorch Docs](https://pytorch.org/docs/)

### Датасеты
- [RoboFlow Universe](https://universe.roboflow.com/) - готовые датасеты
- [COCO Dataset](https://cocodataset.org/) - для pre-training

### Инструменты
- [Netron](https://netron.app/) - визуализация модели
- [ONNX Runtime](https://onnxruntime.ai/) - inference ONNX моделей

---

## ✅ Checklist перед production

- [ ] Собрано 300+ изображений на каждый класс
- [ ] Аннотации проверены на правильность
- [ ] mAP50 > 0.7 на test set
- [ ] Модель экспортирована в ONNX
- [ ] Протестирована inference на реальных данных
- [ ] Измерена скорость inference (FPS)
- [ ] Определены threshold для confidence
- [ ] Настроена интеграция с FastAPI
- [ ] Написаны unit tests
- [ ] Подготовлена документация

---

**Версия:** 1.0
**Дата:** 08.11.2025
**Автор:** Claude Code
