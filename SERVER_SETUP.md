# 🖥️ Настройка компьютера как сервера

## ✅ Что уже настроено

Ваш компьютер теперь работает как публичный сервер!

- ✅ **Demo-сервер запущен** на http://localhost:8000
- ✅ **Публичный туннель** создан на https://stroinadzor-api.loca.lt
- ✅ **API доступен из интернета**
- ✅ **Скрипты автозапуска** созданы

---

## 🚀 Быстрый запуск

### Вариант 1: Запустить все автоматически (Рекомендуется)

Дважды кликните на файл:
```
start_all.bat
```

Это запустит:
1. FastAPI сервер (минимизированное окно)
2. Локальный туннель (показывает публичный URL)
3. Откроет Swagger UI в браузере

### Вариант 2: Запустить вручную

**Шаг 1:** Запустить сервер
```
start_server.bat
```

**Шаг 2:** Создать публичный туннель (опционально)
```
start_tunnel.bat
```

---

## 🔗 Адреса доступа

### Локальный доступ (только с этого компьютера):
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **API:** http://localhost:8000/api/v1

### Публичный доступ (из любой точки мира):
- **Swagger UI:** https://stroinadzor-api.loca.lt/docs
- **API:** https://stroinadzor-api.loca.lt/api/v1

**⚠️ Важно:** При первом доступе к публичному URL может появиться страница предупреждения localtunnel - нажмите "Continue" для доступа к API.

---

## 🔄 Автозапуск при включении компьютера

### Добавить в автозагрузку Windows:
```
add_to_startup.bat
```

После этого сервер будет автоматически запускаться при старте Windows.

### Удалить из автозагрузки:
```
remove_from_startup.bat
```

---

## 📋 Управление сервером

### Остановить сервер:

**Через диспетчер задач:**
1. Открыть Диспетчер задач (Ctrl+Shift+Esc)
2. Найти процессы:
   - `python.exe` (TehNadzor-API)
   - `node.exe` (lt - туннель)
3. Завершить процессы

**Через командную строку:**
```cmd
taskkill /F /IM python.exe /FI "WINDOWTITLE eq TehNadzor-API*"
taskkill /F /IM node.exe /FI "WINDOWTITLE eq TehNadzor-Tunnel*"
```

### Проверить статус сервера:
```cmd
curl http://localhost:8000/docs
```

Если сервер работает - команда вернет HTML код.

---

## 🌐 Поделиться API с другими

Отправьте коллегам ссылки:

**Swagger UI (интерактивная документация):**
```
https://stroinadzor-api.loca.lt/docs
```

**Примеры API запросов:**

```bash
# Получить список проектов
curl https://stroinadzor-api.loca.lt/api/v1/projects

# Статистика дашборда
curl https://stroinadzor-api.loca.lt/api/v1/statistics/dashboard

# Глобальный поиск
curl "https://stroinadzor-api.loca.lt/api/v1/search/global?q=бетон"

# Экспорт в CSV
curl https://stroinadzor-api.loca.lt/api/v1/export/projects/csv -o projects.csv
```

---

## ⚙️ Технические детали

### Запущенные процессы:

1. **FastAPI Demo Server** (`python demo_server.py`)
   - Порт: 8000
   - Режим: Demo (без подключения к БД)
   - 22 endpoints доступны

2. **Localtunnel** (`lt --port 8000 --subdomain stroinadzor-api`)
   - Создает HTTPS туннель к localhost:8000
   - Поддомен: `stroinadzor-api`
   - URL: https://stroinadzor-api.loca.lt

### Преимущества localtunnel:
- ✅ Бесплатный
- ✅ Не требует настройки роутера
- ✅ HTTPS из коробки
- ✅ Работает из-за NAT/firewall

### Недостатки localtunnel:
- ⚠️ URL может измениться при перезапуске (если не указать `--subdomain`)
- ⚠️ Страница предупреждения при первом доступе
- ⚠️ Подходит для разработки/тестирования, не для production

---

## 🔒 Безопасность

### ⚠️ Важные замечания:

1. **Не храните секретные данные** в demo-версии
2. **Не используйте** для production без:
   - Аутентификации (JWT токены)
   - HTTPS с валидным сертификатом
   - Rate limiting
   - Мониторинга

3. **Для production** используйте:
   - Полную версию с PostgreSQL
   - Облачный хостинг (Railway, Render, AWS)
   - Настройку firewall

---

## 🚀 Переход на production

### Рекомендуемые платформы:

**1. Railway (Рекомендуется - самый простой):**
```bash
# Установить Railway CLI
npm install -g @railway/cli

# Залогиниться
railway login

# Создать проект и задеплоить
cd C:\Users\PC\StroiNadzor
railway init
railway up
```

**2. Render:**
- Подключить GitHub репозиторий
- Автоматический деплой при push
- Бесплатный тариф доступен

**3. Docker на VPS:**
```bash
docker-compose -f docker-compose.prod.yml up -d
```

---

## 📞 Помощь

### Логи сервера:

Открыть окно `TehNadzor-API` (может быть минимизировано) чтобы увидеть логи.

### Частые проблемы:

**Сервер не запускается:**
- Проверьте, занят ли порт 8000: `netstat -ano | findstr :8000`
- Убедитесь, что Python установлен: `python --version`
- Установите зависимости: `pip install -r backend/requirements-demo.txt`

**Туннель не работает:**
- Проверьте, запущен ли сервер: `curl http://localhost:8000/docs`
- Переустановите localtunnel: `npm install -g localtunnel`

**Публичный URL не открывается:**
- Нажмите "Continue" на странице предупреждения
- Попробуйте другой поддомен: `lt --port 8000 --subdomain другое-имя`

---

## 📚 Дополнительно

**Документация проекта:**
- [README.md](README.md) - Обзор проекта
- [API_ENDPOINTS.md](API_ENDPOINTS.md) - Список всех endpoints
- [QUICK_START.md](QUICK_START.md) - Быстрый старт для разработки

**GitHub репозиторий:**
https://github.com/AntonKuznetsov1911/StroiNadzor

---

**Ваш компьютер теперь полноценный сервер!** 🎉

Сервер работает 24/7 пока компьютер включен.
